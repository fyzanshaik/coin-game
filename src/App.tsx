import React from 'react';
import CoinTap from './components/CoinTap';

const App: React.FC = () => {
    return (
        <div className="App">
            <CoinTap />
        </div>
    );
};

export default App;
